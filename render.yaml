services:
  - type: web
    name: SmartPostOffice
    env: python
    region: oregon
    branch: main
    buildCommand: "python -m pip install -U pip setuptools wheel && python -m pip install -r requirements.txt"
    startCommand: "./.venv/bin/python -m pip install -U setuptools wheel && ./.venv/bin/gunicorn wsgi:app"
    autoDeploy: true
    healthCheckPath: "/"
